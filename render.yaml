services:
  - type: web
    name: sega-party
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: node ./artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
