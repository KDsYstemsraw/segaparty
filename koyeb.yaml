name: sega-party
services:
  - name: web
    type: web
    instance_type: nano
    regions:
      - was
    ports:
      - port: 3000
        path: /
        protocol: http
    routes:
      - path: /
        port: 3000
    docker:
      dockerfile: Dockerfile
